spec:
        template:
                spec:
                        containers:
                        - envFrom:
                          - configMapRef:
                                name: todo-db-config  
